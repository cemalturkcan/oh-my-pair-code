# plan

Rendered from `createHarnessAgents(loadHarnessConfig(projectRoot))` with the current project/user harness config and discovered installed skills.

## Agent config

| Key | Value |
| --- | --- |
| `disable` | `true` |

## Full prompt

No prompt is defined for this disabled built-in agent.
